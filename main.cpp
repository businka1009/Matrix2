#include "Matrix.hpp"
#include "Rmax.hpp"
#include <iostream>

/*template<typename Type>
Type min(Type a, Type b)
{
	if (a<b)
		return a;
	else return b;
}
*/

void main()
{
	Matrix<Rmax> A(Rmax);
	Matrix<Rmax> B(3,2);
	Matrix<Rmax> C(B);
	//int c,d;
	//std::cin>>c>>d;
	//std::cout<<min(c,d)<<std::endl;
}